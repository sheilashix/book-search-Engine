// new -- add if user hits no existing page
import React from 'react';

const NoMatch = () => {
  return (
    <div>
      Sorry, we couldn't find that page.
    </div>
  );
};

export default NoMatch;